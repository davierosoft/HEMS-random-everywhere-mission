# Runtime test documentation rules

- Add a scenario only when static tooling cannot prove the behavior.
- State setup, observable result, and affected subsystem; avoid release-by-release duplication.
- Preserve the distinction between automated PASS and simulator sign-off.
